class Metrics {
    constructor(top = 0, average = 0, errors=0, points = []) {
      this.top = top;
      this.average = average;
      this.errors = errors;
      this.points = points;

    }
  }
  
  export default Metrics;