name: Feature

on:
  push:
    branches:
      - feature-*

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build Suture Service Backend
      run: docker build -t SutureService/${{ github.ref_name }}:v1 .
